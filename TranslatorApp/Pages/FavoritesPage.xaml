<Page
    x:Class="TranslatorApp.Pages.FavoritesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="16">
        <ListView x:Name="List"
                  IsItemClickEnabled="True"
                  ItemClick="List_ItemClick"
                  SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:FavoriteItem"
                              xmlns:models="using:TranslatorApp.Models">
                    <Grid ColumnDefinitions="*,Auto" Padding="8">
                        <StackPanel>
                            <TextBlock Text="{x:Bind Term}" FontSize="18" />
                            <TextBlock Text="{x:Bind AddedOn, Mode=OneWay}" Opacity="0.6"/>
                        </StackPanel>
                        <Button Grid.Column="1" Content="删除" Click="Delete_Click"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>