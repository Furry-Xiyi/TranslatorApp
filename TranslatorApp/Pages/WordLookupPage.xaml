<Page
    x:Class="TranslatorApp.Pages.WordLookupPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:WebView2="using:Microsoft.UI.Xaml.Controls"
    xmlns:primitives="using:TranslatorApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <ResourceDictionary>
            <!-- 按钮与骨架资源 -->
            <SolidColorBrush x:Key="PlayButtonForeground" Color="White"/>
            <SolidColorBrush x:Key="PlayButtonBgNormal" Color="{ThemeResource SystemAccentColor}" Opacity="0.18"/>
            <SolidColorBrush x:Key="PlayButtonBgActive" Color="{ThemeResource SystemAccentColor}" Opacity="0.70"/>

            <!-- 更轻盈的骨架底色（接近微软商店） -->
            <SolidColorBrush x:Key="SkeletonFillBrush"
                 Color="{ThemeResource SystemAccentColor}"
                 Opacity="0.08"/>

            <!-- Shimmer 默认样式：提供命名部件 Shape 作为扫光宿主 -->
            <Style TargetType="primitives:Shimmer">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="primitives:Shimmer">
                            <Grid>
                                <!-- 内容（通常留空，仅用于撑开尺寸时可以放占位元素） -->
                                <ContentPresenter/>
                                <!-- 扫光画布（命名部件） -->
                                <Border x:Name="Shape"
                                        Background="{TemplateBinding Background}"
                                        CornerRadius="{TemplateBinding CornerRadius}"
                                        IsHitTestVisible="False"/>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 顶部卡片（覆盖两行，位于最上层） -->
        <Border
            x:Name="DailySentenceCard"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            CornerRadius="8"
            Margin="12"
            Padding="12"
            Grid.Row="0"
            Grid.RowSpan="2"
            VerticalAlignment="Stretch"
            Visibility="Visible"
            Canvas.ZIndex="2">

            <Border.Shadow>
                <ThemeShadow/>
            </Border.Shadow>

            <Grid x:Name="CardGrid">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <!-- Caption -->
                    <RowDefinition Height="Auto"/>
                    <!-- Date -->
                    <RowDefinition Height="Auto"/>
                    <!-- En + TTS -->
                    <RowDefinition Height="Auto"/>
                    <!-- Zh -->
                    <RowDefinition Height="*"/>
                    <!-- Image -->
                    <RowDefinition Height="Auto"/>
                    <!-- 预留 -->
                </Grid.RowDefinitions>

                <!-- Caption 骨架 + 文本 -->
                <Grid Grid.Row="0" Height="20">
                    <primitives:Shimmer x:Name="CaptionShimmer"
                                        IsActive="False"
                                        IsHitTestVisible="False"
                                        Background="{StaticResource SkeletonFillBrush}"
                                        CornerRadius="4"/>
                    <TextBlock x:Name="TxtCaption"
                               Style="{ThemeResource BodyStrongTextBlockStyle}"
                               Foreground="{ThemeResource TextFillColorPrimaryBrush}"
                               Visibility="Collapsed"/>
                </Grid>

                <!-- Date 骨架 + 文本 -->
                <Grid Grid.Row="1" Margin="0,4,0,0" Height="16">
                    <primitives:Shimmer x:Name="DateShimmer"
                                        IsActive="False"
                                        IsHitTestVisible="False"
                                        Background="{StaticResource SkeletonFillBrush}"
                                        CornerRadius="4"
                                        Width="120"
                                        HorizontalAlignment="Left"/>
                    <TextBlock x:Name="TxtDate"
                               Style="{ThemeResource CaptionTextBlockStyle}"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                               Visibility="Collapsed"/>
                </Grid>

                <!-- 英文骨架 + 喇叭 -->
                <Grid Grid.Row="2" Margin="0,8,0,0">
                    <primitives:Shimmer x:Name="EnShimmer"
                                        IsActive="False"
                                        IsHitTestVisible="False"
                                        Background="{StaticResource SkeletonFillBrush}"
                                        CornerRadius="4"
                                        Height="32"
                                        HorizontalAlignment="Stretch"/>
                    <StackPanel Orientation="Horizontal"
                                VerticalAlignment="Center"
                                Spacing="8"
                                Visibility="Collapsed"
                                x:Name="ControlPanel">
                        <TextBlock x:Name="TxtEn"
                                   TextWrapping="WrapWholeWords"
                                   Style="{ThemeResource BodyTextBlockStyle}"
                                   FontSize="20"
                                   Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                        <Button x:Name="BtnPlayTts"
                                Width="32" Height="32"
                                CornerRadius="8"
                                Background="{StaticResource PlayButtonBgNormal}"
                                Foreground="{StaticResource PlayButtonForeground}"
                                ToolTipService.ToolTip="播放发音">
                            <SymbolIcon Symbol="Volume"/>
                        </Button>
                    </StackPanel>
                </Grid>

                <!-- 中文骨架 + 文本 -->
                <Grid Grid.Row="3" Height="16" Margin="0,4,0,0">
                    <primitives:Shimmer x:Name="ZhShimmer"
                                        IsActive="False"
                                        IsHitTestVisible="False"
                                        Background="{StaticResource SkeletonFillBrush}"
                                        CornerRadius="4"/>
                    <TextBlock x:Name="TxtZh"
                               TextWrapping="WrapWholeWords"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                               Visibility="Collapsed"/>
                </Grid>

                <!-- 图片骨架 + 图片 -->
                <Grid Grid.Row="4"
                      CornerRadius="6"
                      Margin="0,4,0,4"
                      MinHeight="528"
                      Height="528"
                      Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                    <primitives:Shimmer x:Name="ImageShimmer"
                                        IsActive="False"
                                        IsHitTestVisible="False"
                                        Background="{StaticResource SkeletonFillBrush}"
                                        CornerRadius="6"/>
                    <Image x:Name="ImgPic"
                           Stretch="Uniform"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Visibility="Collapsed"/>
                </Grid>

            </Grid>
        </Border>

        <!-- WebView 区域（在底层） -->
        <Grid Grid.Row="1" Canvas.ZIndex="0">
            <Border x:Name="WebMask"
                    Background="{ThemeResource SolidBackgroundFillColorBaseBrush}"
                    Visibility="Collapsed"/>
            <WebView2:WebView2 x:Name="Web"
                               NavigationCompleted="Web_NavigationCompleted"
                               Visibility="Collapsed"
                               DefaultBackgroundColor="Transparent"/>
            <Button x:Name="FabFavorite"
                    Click="FabFavorite_Click"
                    Width="56" Height="56"
                    CornerRadius="8"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Bottom"
                    Margin="16"
                    ToolTipService.ToolTip="收藏当前查询词汇"
                    Visibility="Collapsed">
                <Button.Background>
                    <AcrylicBrush TintColor="{ThemeResource SystemAccentColor}"
                                  TintOpacity="0.4"
                                  FallbackColor="{ThemeResource SystemAccentColor}"/>
                </Button.Background>
                <SymbolIcon Symbol="Favorite" Foreground="White"/>
            </Button>
        </Grid>
    </Grid>
</Page>